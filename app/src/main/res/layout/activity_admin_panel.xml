<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#B9DCB9">

    <!-- Círculo decorativo de fondo (Opcional para dar profundidad) -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="30dp"
        android:gravity="center_horizontal">

        <ImageView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginTop="40dp"
            android:src="@android:drawable/ic_lock_lock"
            app:tint="@android:color/white" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Admin Panel"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_marginTop="16dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Gestión de Espacios Públicos"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:layout_marginBottom="50dp"/>

        <!-- Botón: Nuevo Parque -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAltaParque"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:text="@string/btn_alta_parque"
            app:cornerRadius="16dp"
            android:textColor="@android:color/white"
            android:backgroundTint="#8BC34A"
            app:icon="@android:drawable/ic_menu_mapmode"
            app:iconPadding="12dp"
            android:layout_marginBottom="16dp"
            style="@style/Widget.MaterialComponents.Button.UnelevatedButton"/>

        <!-- Botón: Nuevo Admin -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAltaAdmin"
            android:layout_width="match_parent"
            android:layout_height="70dp"
            android:text="@string/btn_nuevo_admin"
            app:cornerRadius="16dp"
            android:textColor="@android:color/white"
            android:backgroundTint="#558B2F"
            app:icon="@android:drawable/ic_menu_add"
            app:iconPadding="12dp"
            android:layout_marginBottom="16dp"
            style="@style/Widget.MaterialComponents.Button.UnelevatedButton"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#90FFFFFF"
            android:layout_marginVertical="20dp"/>

        <!-- Botón Salir -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLogout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_cerrar_sesion"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:textColor="@android:color/white"
            app:icon="@android:drawable/ic_lock_power_off"
            app:iconTint="@android:color/white"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>